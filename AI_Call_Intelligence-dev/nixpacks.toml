[phases.setup]
nixPkgs = ["ffmpeg", "chromium", "python312"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "playwright install chromium --with-deps"
]

[start]
cmd = "uvicorn app.realtime_server:app --host 0.0.0.0 --port $PORT"
